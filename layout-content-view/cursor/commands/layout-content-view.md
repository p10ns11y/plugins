# /layout-content-view (Cursor)

Load **layout-content-view**. Same contract as the Grok command (no `$ARGUMENTS` — use the user turn).

1. Feature-map paths; named viewports only.
2. Classify with `scripts/lcv.mjs` predicates, not PNG diff.
3. One recipe per shot. Preview overflow is not a fail.
4. Compose with the repo verify skill; do not duplicate route catalogs.
