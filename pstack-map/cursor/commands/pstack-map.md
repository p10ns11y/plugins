# /pstack-map (Cursor)

Load **pstack-map**. Same contract as the Grok command (no `$ARGUMENTS` — use the user turn).

Prefer installed **pstack** `/poteto-mode` for Cursor-native work. Use this map when you need Grok house hooks (control-graph, EVA, HITL).

1. Match one playbook (or skip).
2. Load pstack if present; else house fallback.
3. HITL on secrets / prod / irreversible git / unknown auth.
4. Do not copy pstack files. Credit Lauren Tan (MIT).

Emit the **Pstack-map** table.
