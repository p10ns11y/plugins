---
description: Pulse instead of dump — tag claims, resolve contradictions, inject sparse memory snippets.
---

# /pulse-memory

Load skill **pulse-memory**. Same contract as the Grok command (no `$ARGUMENTS` — use the user turn).

1. Tag every claim (`source`, `time`, `status`, `kind`, `lock`).
2. Admit only traces that change the next action.
3. Do not average contradictions.
4. Pulse snippets, not dumps.
