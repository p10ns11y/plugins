---
description: Place one invariant on shape, check, watch, skill, or a human guide. The codebase is the memory. This command does not merge.
argument-hint: the invariant, the repeated miss, or the change you might fan out
---

# /trust-stack

Load skill **trust-stack**. Open `references/layers.md` only to pick one row.

`$ARGUMENTS` is the invariant. If it is empty, use the current turn.

## Immediate actions

1. Walk `shape` → `check` → `watch` → `skill` → `guide`. Stop at the first layer that can hold the invariant.
2. `guide` means hitl. Do not automate taste.
3. If a bad pattern is spreading, delete it. Write the lesson into a check or a skill. Do not add an excuse comment.
4. Name one verify command the agent can run. Missing command → the next step is to add it.
5. One owner for the change. This command does not merge.

## Emit (required)

```markdown
## Trust
| Field | Value |
|-------|--------|
| **invariant** | |
| **layer** | shape · check · watch · skill · guide |
| **contagion** | |
| **verify** | |
| **owner** | |
| **hitl** | none · required |
| **next** | |
```

Credit Lauren Tan / pstack MIT when a playbook runs. The talk credit is in `NOTICE.md`.
